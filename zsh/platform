# vim: ft=zsh

PLATFORM='unknown'

case $(uname) in
    Linux)
        PLATFORM='linux'
        ;;
    Darwin)
        PLATFORM='macos'
        ;;
esac

case "$PLATFORM" in
    macos)
        source "$HOME/.zsh/profiles/osx"
        ;;
esac
